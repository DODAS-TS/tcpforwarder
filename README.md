# tcpforwarder

A simple tcp forwarder program written in Go.

## :rocket: Quick start

Build the program:

```bash
go build .
```

Run the program:

```bash
./tcpforward -i anip2listen:port -o anip2forward:port
```
